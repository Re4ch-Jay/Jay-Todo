<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Jay-Todo</title>
        <!-- CSS only -->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-iYQeCzEYFbKjA/T2uDLTpkwGzCiq6soy8tYaI1GyVh/UjpbCx/TYkiZhlZB6+fzT" crossorigin="anonymous">
        <link rel="stylesheet" href="style.css">
    </head>
<body>
    <div class="img text-center">
        <img src="logo.png" alt="" class="img m-0">
    </div>
   
    <div class="text-center align-items-center container-sm">
        <form action="/todo" method="POST">
            <input class="form-control p-4 text-center fs-5" placeholder="Add Todo" type="text" name="todo" id="todo" required>
            <button class="btn btn-danger btn-lg my-5">Add Todo</button>
        </form>
    </div>
    <div class="text-center container-sm">
        <% if(todos.length > 0){ %> 
            <% todos.forEach(todo => { %> 
                <div class="card mb-5">
                    <a class="card-body" href="/todo/<%= todo._id %>">
                        <h3 class="card-title text-danger fw-bold"><%= todo.todo %></h3>
                        <p class="card-text text-danger"><%= todo.createdAt %> </p>
                    </a>
                </div>
            <% }) %> 
        <% }else { %> 
            <h3 class="text">No todos left, Yayyy</h3>
            <% } %> 
    </div>

    <footer>
        <p class="text-center text-muted">Copyright 2022 @Phat Panhareach</p>
    </footer>
    
</body>
</html>